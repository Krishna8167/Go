// Execution speed & compilation speed : go > (JS,python,ruby,php)
/* go have fast runtime speed , go runtime there's basically a chunk of code
   that's included in every go that manages memory and thats tends to
   slow down the execution speed a little bit
*/
package main

import "fmt"

func main() {
	fmt.Println("hello world")
}
